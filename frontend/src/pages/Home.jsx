import { useEffect, useState } from 'react';
import { motion } from 'framer-motion';
import { ArrowRight, Atom, BookOpen, Sparkles } from 'lucide-react';
import { Link } from 'react-router-dom';
import Layout from '../components/Layout.jsx';
import LectureCard from '../components/LectureCard.jsx';
import { api, messageFromError } from '../lib/api.js';

export default function Home() {
  const [settings, setSettings] = useState(null); const [lectures, setLectures] = useState([]); const [error, setError] = useState('');
  useEffect(() => { Promise.all([api.get('/settings'), api.get('/lectures/public')]).then(([s,l]) => { setSettings(s.data.data.settings); setLectures(l.data.data.lectures.slice(0,3)); }).catch(e => setError(messageFromError(e))); }, []);
  return <Layout><section className="grid-bg relative overflow-hidden"><div className="mx-auto grid min-h-[650px] max-w-7xl items-center gap-10 px-5 py-20 lg:grid-cols-2"><motion.div initial={{opacity:0,y:25}} animate={{opacity:1,y:0}} transition={{duration:.7}}><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-cyan-300/20 bg-cyan-300/10 px-4 py-2 text-sm text-cyan-200"><Sparkles size={16}/> Learn smarter. Go further.</div><h1 className="max-w-3xl text-5xl font-black leading-tight text-white md:text-7xl">{settings?.headline || 'Chemistry & Science made simple.'}</h1><p className="mt-6 max-w-xl text-lg leading-8 text-slate-400">A focused learning space for students to follow lessons in order, track progress, and build real understanding.</p><div className="mt-8 flex flex-wrap gap-3"><Link to="/login" className="flex items-center gap-2 rounded-2xl bg-cyan-400 px-6 py-3 font-bold text-slate-950">Start Learning <ArrowRight size={18}/></Link><a href="#lectures" className="rounded-2xl border border-white/10 px-6 py-3 font-semibold text-white hover:bg-white/5">Explore Lectures</a></div></motion.div><motion.div initial={{opacity:0,scale:.95}} animate={{opacity:1,scale:1}} transition={{duration:.8}} className="relative"><div className="absolute -inset-10 rounded-full bg-cyan-400/10 blur-3xl"/><div className="relative mx-auto max-w-md overflow-hidden rounded-[2rem] border border-white/10 bg-slate-900/70 shadow-2xl shadow-cyan-950/30">{settings?.teacherImageUrl ? <img src={settings.teacherImageUrl} alt={settings.teacherName} className="aspect-[4/5] w-full object-cover"/> : <div className="grid aspect-[4/5] place-items-center bg-gradient-to-br from-cyan-400/20 to-blue-500/20"><Atom size={100} className="text-cyan-300/70"/></div>}<div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-slate-950 to-transparent p-7 pt-24"><p className="text-sm text-cyan-300">Your Teacher</p><h2 className="text-2xl font-bold text-white">{settings?.teacherName || 'Dr. Your Name'}</h2></div></div></motion.div></div></section><section className="mx-auto max-w-7xl px-5 py-24"><div className="grid gap-5 md:grid-cols-3"><Feature icon={<BookOpen/>} title="Structured Lessons" text="Lessons unlock in order so students always know what to study next."/><Feature icon={<Atom/>} title="Science Focused" text="Clear, organized content built around chemistry and science fundamentals."/><Feature icon={<Sparkles/>} title="Track Progress" text="Every completed lesson is saved to the student's account."/></div></section><section id="lectures" className="mx-auto max-w-7xl px-5 pb-24"><div className="mb-8 flex items-end justify-between"><div><p className="text-sm font-bold uppercase tracking-[.25em] text-cyan-300">Latest</p><h2 className="mt-2 text-3xl font-bold text-white">Featured Lectures</h2></div><Link to="/login" className="text-sm text-cyan-300">Student login →</Link></div>{error ? <p className="text-rose-300">{error}</p> : lectures.length ? <div className="grid gap-5 md:grid-cols-3">{lectures.map(l => <LectureCard key={l.id} lecture={l}/>)}</div> : <div className="glass rounded-2xl p-8 text-slate-400">Lectures will appear here after the admin publishes them.</div>}</section><section className="mx-auto max-w-7xl px-5 pb-24"><div className="glass rounded-3xl p-8 md:p-12"><h2 className="text-3xl font-bold text-white">{settings?.teacherName || 'Your Teacher'}</h2><p className="mt-4 max-w-3xl leading-8 text-slate-400">{settings?.bio || 'Welcome to the academy. This space is designed to make learning organized, focused, and enjoyable.'}</p></div></section></Layout>;
}
function Feature({icon,title,text}) { return <div className="glass rounded-2xl p-6"><div className="mb-5 grid h-11 w-11 place-items-center rounded-xl bg-cyan-400/10 text-cyan-300">{icon}</div><h3 className="font-bold text-white">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-400">{text}</p></div>; }
